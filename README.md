# A Simple pomodoro app build with React, Typescript,and RxJs.

Simple pomodoro app which can start, stop,and reset. For the countdown behavior, _**RxJs**_ is used instead of traditional **setInterval** API.
